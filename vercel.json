{
  "routes": [
      {
        "src": "/.*",
        "headers":
          { "key": "Access-Control-Allow-Origin", "value": "*" },
        "dest": "/api/(.*)"
      },
      {
        "src": "/api/(.*)",
        "headers":
          { "key": "Access-Control-Allow-Origin", "value": "*" },
        "dest": "api/app.py"
      }
    ]
  }